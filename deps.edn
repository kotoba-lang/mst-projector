{:paths ["src"]
 :deps {
        ;; kotoba stdlib, not clojure.*. Pinned as explicit floors: tools.deps
        ;; takes the NEWEST sha it is shown, so without a floor here this repo
        ;; silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/edn {:git/sha "b8a2a05876638442270956334e1e0d6328531b1b"}
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
              ;; Advanced from b47b064: that pin predates json's kwargs fix (d5137c7),
       ;; so `(read-str s :key-fn keyword)` threw ArityException at every call
       ;; site. The fix was on json's main and could not reach here.
       io.github.kotoba-lang/json {:git/sha "15fd195ac41e6268e2005ccbf6cb9924e81e65e2"}}
 :aliases
 {:test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd6605cb6c0efc275e1df1736f6e90d4d73"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  :lint {:extra-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts ["-m" "clj-kondo.main" "--lint" "src" "test"
                     "--fail-level" "error"]}
  :run {:main-opts ["-m" "kotoba.lang.mst-projector.cli"]}}}
